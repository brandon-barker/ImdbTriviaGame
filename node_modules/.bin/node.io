#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node.io/bin/node.io" "$@"
  ret=$?
else 
  node  "$basedir/../node.io/bin/node.io" "$@"
  ret=$?
fi
exit $ret
